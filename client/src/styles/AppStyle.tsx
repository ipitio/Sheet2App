import createTheme from "@mui/material/styles/createTheme";

// Defines the app style for material ui, including default colors and component properties
export const style = createTheme({
    palette: {
        primary: {
            main: "#97E4FF"
        }
    }
})